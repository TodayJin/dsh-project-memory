# Bundle patch layer: the row loads this package's main entry as a host plugin.
# `dsh.bundle.patch` in package.json points here, so `dsh plugin add` picks the
# entry up without any manual editing of the profile.
- insert:
    - id: trilogy
      name: dsh-trilogy
